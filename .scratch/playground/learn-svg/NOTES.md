# Notes on how Mihir learns

- **Style:** explicit, step-by-step sequencing. Announce the arc; don't jump ahead.
- **JS/DOM (self-rated):** *Somewhat* — can read/get by, not second nature.
  → Introduce `createElementNS` and JS-generated SVG gradually, with commented code.
  Don't assume fluency; explain each DOM call the first few times.
- **Trig (self-rated):** *Half-remembered* — seen sin/cos, wants a refresher.
  → Lesson 3 (rotation) MUST embed a short, visual sin/cos + radians refresher before
  the rotation matrix. Same when we reach isometric projection math.
- **Theme:** prefers **dark theme** for lessons (set in `assets/course.css` palette vars).
  Drawing-canvas surfaces (`.figure`, `.tryit .out`) stay light on purpose so SVG art
  reads well; print styles force light for toner/legibility.
- Grounds everything in his own prototype — keep tying lessons back to it; it's motivating.
